import React from 'react';
import Chat from '../components/Chat.tsx'; // Adjust the import path as needed

const ChatPage = () => {
    return (
        <section className="section">
            <div className="container">
                <Chat />
            </div>
        </section>
    );
};

export default ChatPage;
